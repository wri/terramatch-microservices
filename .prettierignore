# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
/data
# Entire geoserver tree: Docker writes root-owned tiles Prettier cannot scandir
/docker
.nx/self-healing