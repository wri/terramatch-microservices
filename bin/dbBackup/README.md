The files in this directory are not used for local development. Instead, they represent a daily script
that is run on the bastion host (geoserver - see bastion-instance-id.sh). See /etc/crontab on the
bastion host for the schedule.
